/*
 Write a script that reads an integer number n from the console and prints
 all numbers in the range [1 - n], each on a separate line.
 */

var number = prompt("What si your number:");

for(var i = 1; i <= number; i++) {
    console.log(i);
}
